#include<stdio.h>
void tinhMin{int min }{
if(min>a[i]){
    min=a[i];
} return 0;
}

int main(){
    int a[100]={1,2,3,4,3,7};
    int i;
    int max=a[0];
    for(i=1;i<6;i++){
        if(max<a[i]){
            max=a[i];
        }
    }
   
     printf("%d",max);
     
     tinhMin(a[]);
return 0;}

